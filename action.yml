name: Distr App Upload Action
description: Provide a description here
author: Your name or organization here

# Add your action's branding here. This will appear on the GitHub Marketplace.
branding:
  icon: heart
  color: red

# Define your inputs here.
inputs:
  distr-api-base:
    description: ID of the application that the version will be created in
    required: true
  distr-api-token:
    description: ID of the application that the version will be created in
    required: true
  distr-application-id:
    description: ID of the application that the version will be created in
    required: true
  distr-application-version-name:
    description: Name of the version that will be created
    required: true
  distr-application-version-compose-file:
    description: Compose File Contents
    required: false

# Define your outputs here.
outputs:
  version-id:
    description: Your output description here

runs:
  using: node20
  main: dist/index.js
